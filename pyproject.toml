[project]
name = "pokemmo-mcts-engine-starter"
version = "0.1.0"
description = "Scoped PokeMMO-style battle engine starter for MCTS/ML experiments"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
